[package]
name = "game"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["dylib"]

[dependencies]
shared = { path = "../shared" }

allocator-api2 = { workspace = true }
anyhow = { workspace = true }
sdl3 = { workspace = true }


[features]
nightly = []
