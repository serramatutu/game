disallowed-methods = [
    { path = "allocator_api2::vec::Vec::new", reason = "Must use explicit allocator" },
    { path = "std::vec::Vec::with_capacity", reason = "Must use explicit allocator" },

    { path = "hashbrown::HashMap::new", reason = "Must use explicit allocator" },
    { path = "hashbrown::HashSet::new", reason = "Must use explicit allocator" }
]

disallowed-types = [
    { path = "std::vec::Vec", reason = "Use allocator_api2::vec::Vec or heapless::Vec instead" },
    { path = "std::collections::HashMap", reason = "Use hashbrown::HashMap instead" },
    { path = "std::collections::HashSet", reason = "Use hashbrown::HashSet instead" }
]
